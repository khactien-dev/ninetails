import { Typography } from 'antd';
import styled from 'styled-components';

export const BaseParagraph = styled(Typography.Paragraph)`
  color: unset;
  margin-bottom: 0 !important;
  font-size: unset;
`;
